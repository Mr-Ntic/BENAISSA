#ifndef PARSING_H
#define PARSING_H

#include <QMainWindow>

class parsing : public QMainWindow
{
    Q_OBJECT
public:
    explicit parsing(QWidget *parent = 0);

signals:

public slots:
};

#endif // PARSING_H